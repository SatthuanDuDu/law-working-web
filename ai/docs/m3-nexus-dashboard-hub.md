# M3 Nexus — Slice 2: dashboard + matter hub density

Density and hierarchy pass using **existing data only** (no Wiki/HR/Kanban features).

## Dashboard

- Greeting hero with `pages.dashboard.greeting` + description
- Quick-action pills → `/tasks`, `/calendar`, `/matters`
- KPI cards: `rounded-2xl`, `p-4`, larger tonal icon pill
- Status distribution progress bars: `h-2` M3 style

## Matter hub

- Sidebar info column spacing `space-y-4`
- Main column `space-y-5`
- Progress + roadmap cards `rounded-2xl`
- Step cards `rounded-2xl`, icon buttons `rounded-full`
- Comments card `rounded-2xl`

## Unchanged

- All queries, counts, routes
- Matter overview data model
- Nav / IA labels
