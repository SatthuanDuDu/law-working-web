# UI density pass

## Done
- Card default `p-4` (was `p-6`)
- List grids `/tasks` `/matters` `/clients`: `gap-2`, item `p-3`, no equal-height stretch
- Dashboard: `space-y-4`, section `gap-3`; SectionPanel `p-2.5 sm:p-3`
- Page chrome: `main` `lg:px-6 lg:pt-5 lg:pb-6`; calendar/website flush nav margins synced
- Admin 2-col `gap-6`; website panels `p-4` / stack `space-y-5`; modal body `space-y-4`

## Rules
See density section in `ai/docs/ui-primitives.md`. Calendar viewport lock unchanged.
